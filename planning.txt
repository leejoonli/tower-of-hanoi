/*----- constants -----*/
disks

/*----- app's state (variables) -----*/
disk div containers
tower div containers

/*----- cached element references -----*/
about game button
input
start button
disk div element
tower div element
tower container div element
reset button

/*----- event listeners -----*/
start button
    init function
    render function
reset button
    render function
about game button
    modal to display rules of game
disk div element
    select which div element
    select where to put it
        check to see if valid move <- YOU ARE HERE
            move disk div element to tower div element
            check win condition
                if win then end game
                else continue
        else do nothing

/*----- functions -----*/

init function [x] for now

render function [x] for now
 
select function [x] for now

track function <- not implementing currently

move function <- YOU ARE HERE

win function