/*----- constants -----*/
disk class

/*----- app's state (variables) -----*/
disk div containers
tower div containers -> array

/*-----------------------------------*/
    stackOne arr
    stackTwo arr
    stackThree arr
/*-----------------------------------*/

/*----- cached element references -----*/
about game button
input
start button
disk div element
tower div element
tower container div element
reset button

/*----- event listeners -----*/
start button
    init function
    render function
reset button
    render function
about game button
    modal to display rules of game
disk div element
    select which div element
    select where to put it and change the array to match <- YOU ARE HERE
        check to see if valid move
            move disk div element to tower div element
            check win condition
                if win then end game
                else continue
        else do nothing

make objects instead of dom elements to store data
make div disk elements
set innerText of div disk elements to objects key/property
push it to the stackOne array
    when div disk element moves change the arrays to match the html <- YOU ARE HERE

/*----- functions -----*/

init function [x] for now

render function [x] for now
 
select function [x] for now

track function <- not implementing in current version

move function <- YOU ARE HERE

win function